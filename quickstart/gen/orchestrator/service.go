// Code generated by goa, DO NOT EDIT.
//
// orchestrator service
//
// Command:
// $ goa gen example.com/quickstart/design

package orchestrator

import (
	bytes "bytes"
	json "encoding/json"
	fmt "fmt"
	io "io"
	sort "sort"
	strconv "strconv"
	utf8 "unicode/utf8"

	goa "goa.design/goa/v3/pkg"
)

// Service is the orchestrator service interface.
type Service interface {
}

// APIName is the name of the API as defined in the design.
const APIName = "orchestrator"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "orchestrator"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [0]string{}

type Answer struct {
	// Answer text
	Text string
}

type AskPayload struct {
	// User question to answer
	Question string
}

// jsonAnswerTransport stores JSON fields until they have been validated.
type jsonAnswerTransport struct {
	// Answer text
	Text *string `json:"text"`
}

// validatejsonAnswerTransport checks decoded JSON before it becomes a service value.
func validatejsonAnswerTransport(value *jsonAnswerTransport) (err error) {
	if value == nil {
		return goa.MissingFieldError("body", "JSON value")
	}
	if value.Text == nil {
		err = goa.MergeErrors(err, goa.MissingFieldError("text", "body"))
	}
	return err
}

// jsonAskPayloadTransport stores JSON fields until they have been validated.
type jsonAskPayloadTransport struct {
	// User question to answer
	Question *string `json:"question"`
}

// validatejsonAskPayloadTransport checks decoded JSON before it becomes a service value.
func validatejsonAskPayloadTransport(value *jsonAskPayloadTransport) (err error) {
	if value == nil {
		return goa.MissingFieldError("body", "JSON value")
	}
	if value.Question == nil {
		err = goa.MergeErrors(err, goa.MissingFieldError("question", "body"))
	}
	return err
}

// EncodeAnswer turns a service value into JSON using the field names in the Goa design.
func EncodeAnswer(in *Answer) ([]byte, error) {
	if err := checkAnswerValue(in); err != nil {
		return nil, fmt.Errorf("encode Answer JSON: %w", err)
	}
	var body *jsonAnswerTransport
	body = &jsonAnswerTransport{
		Text: &in.Text,
	}
	if err := validatejsonAnswerTransport(body); err != nil {
		return nil, fmt.Errorf("validate Answer JSON: %w", err)
	}
	data, err := json.Marshal(body)
	if err != nil {
		return nil, fmt.Errorf("encode Answer JSON: %w", err)
	}
	return data, nil
}

// DecodeAnswer checks JSON field names from the Goa design and returns a service value.
func DecodeAnswer(data []byte) (out *Answer, err error) {
	root, err := readStrictJSON(data)
	if err != nil {
		return out, fmt.Errorf("decode Answer JSON: %w", err)
	}
	if err := validateAnswerJSONValue("", root, ""); err != nil {
		return out, fmt.Errorf("decode Answer JSON: %w", err)
	}
	var body *jsonAnswerTransport
	decoder := json.NewDecoder(bytes.NewReader(data))
	decoder.DisallowUnknownFields()
	if err := decoder.Decode(&body); err != nil {
		return out, fmt.Errorf("decode Answer JSON: %w", err)
	}
	if err := decoder.Decode(&struct{}{}); err != io.EOF {
		if err == nil {
			return out, fmt.Errorf("decode Answer JSON: multiple JSON values")
		}
		return out, fmt.Errorf("decode Answer JSON after first value: %w", err)
	}
	if err := validatejsonAnswerTransport(body); err != nil {
		return out, fmt.Errorf("validate Answer JSON: %w", err)
	}
	out = &Answer{
		Text: *body.Text,
	}
	return out, nil
}

// EncodeAskPayload turns a service value into JSON using the field names in the Goa design.
func EncodeAskPayload(in *AskPayload) ([]byte, error) {
	if err := checkAskPayloadValue(in); err != nil {
		return nil, fmt.Errorf("encode AskPayload JSON: %w", err)
	}
	var body *jsonAskPayloadTransport
	body = &jsonAskPayloadTransport{
		Question: &in.Question,
	}
	if err := validatejsonAskPayloadTransport(body); err != nil {
		return nil, fmt.Errorf("validate AskPayload JSON: %w", err)
	}
	data, err := json.Marshal(body)
	if err != nil {
		return nil, fmt.Errorf("encode AskPayload JSON: %w", err)
	}
	return data, nil
}

// DecodeAskPayload checks JSON field names from the Goa design and returns a service value.
func DecodeAskPayload(data []byte) (out *AskPayload, err error) {
	root, err := readStrictJSON(data)
	if err != nil {
		return out, fmt.Errorf("decode AskPayload JSON: %w", err)
	}
	if err := validateAskPayloadJSONValue("", root, ""); err != nil {
		return out, fmt.Errorf("decode AskPayload JSON: %w", err)
	}
	var body *jsonAskPayloadTransport
	decoder := json.NewDecoder(bytes.NewReader(data))
	decoder.DisallowUnknownFields()
	if err := decoder.Decode(&body); err != nil {
		return out, fmt.Errorf("decode AskPayload JSON: %w", err)
	}
	if err := decoder.Decode(&struct{}{}); err != io.EOF {
		if err == nil {
			return out, fmt.Errorf("decode AskPayload JSON: multiple JSON values")
		}
		return out, fmt.Errorf("decode AskPayload JSON after first value: %w", err)
	}
	if err := validatejsonAskPayloadTransport(body); err != nil {
		return out, fmt.Errorf("validate AskPayload JSON: %w", err)
	}
	out = &AskPayload{
		Question: *body.Question,
	}
	return out, nil
}

// validateAnswerJSONValue checks one value whose JSON shape is fixed by the generated Goa type.
func validateAnswerJSONValue(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "object", "null", description)
	}
	typed, ok := value.(map[string]any)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "object", decodedJSONType(value), description)
	}
	keys := make([]string, 0, len(typed))
	for key := range typed {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		switch key {
		case "text":
			if err := validateAnswerJSONValue2(
				generatedJSONChildPath(path, key, false),
				typed[key], "Answer text",
			); err != nil {
				return err
			}
		default:
			return unknownJSONFieldError(path, key, []string{
				"text",
			})
		}
	}
	return nil
}

// validateAnswerJSONValue2 checks one value whose JSON shape is fixed by the generated Goa type.
func validateAnswerJSONValue2(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "string", "null", description)
	}
	_, ok := value.(string)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "string", decodedJSONType(value), description)
	}
	return nil
}

// validateAskPayloadJSONValue checks one value whose JSON shape is fixed by the generated Goa type.
func validateAskPayloadJSONValue(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "object", "null", description)
	}
	typed, ok := value.(map[string]any)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "object", decodedJSONType(value), description)
	}
	keys := make([]string, 0, len(typed))
	for key := range typed {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		switch key {
		case "question":
			if err := validateAskPayloadJSONValue2(
				generatedJSONChildPath(path, key, false),
				typed[key], "User question to answer",
			); err != nil {
				return err
			}
		default:
			return unknownJSONFieldError(path, key, []string{
				"question",
			})
		}
	}
	return nil
}

// validateAskPayloadJSONValue2 checks one value whose JSON shape is fixed by the generated Goa type.
func validateAskPayloadJSONValue2(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "string", "null", description)
	}
	_, ok := value.(string)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "string", decodedJSONType(value), description)
	}
	return nil
}

// readStrictJSON checks syntax and nesting before walking for duplicate keys,
// and preserves integer text for the generated type checks.
func readStrictJSON(data []byte) (any, error) {
	if err := validateJSONText(data); err != nil {
		return nil, err
	}
	if !json.Valid(data) {
		return nil, fmt.Errorf("invalid JSON syntax or nesting")
	}
	decoder := json.NewDecoder(bytes.NewReader(data))
	decoder.UseNumber()
	return readJSONValue(decoder)
}

// readJSONValue checks duplicate decoded keys before storing any object member.
func readJSONValue(decoder *json.Decoder) (any, error) {
	token, err := decoder.Token()
	if err != nil {
		return nil, err
	}
	switch token {
	case json.Delim('{'):
		object := make(map[string]any)
		for decoder.More() {
			token, err := decoder.Token()
			if err != nil {
				return nil, err
			}
			key, ok := token.(string)
			if !ok {
				return nil, fmt.Errorf("object member name must be a string")
			}
			if _, exists := object[key]; exists {
				return nil, fmt.Errorf("duplicate JSON member %q", key)
			}
			value, err := readJSONValue(decoder)
			if err != nil {
				return nil, err
			}
			object[key] = value
		}
		if _, err := decoder.Token(); err != nil {
			return nil, err
		}
		return object, nil
	case json.Delim('['):
		array := make([]any, 0)
		for decoder.More() {
			value, err := readJSONValue(decoder)
			if err != nil {
				return nil, err
			}
			array = append(array, value)
		}
		if _, err := decoder.Token(); err != nil {
			return nil, err
		}
		return array, nil
	default:
		if _, delimiter := token.(json.Delim); delimiter {
			return nil, fmt.Errorf("unexpected JSON delimiter %v", token)
		}
		return token, nil
	}
}

// validateJSONText rejects invalid UTF-8 and unpaired UTF-16 escapes before
// encoding/json can silently replace them. JSON grammar remains decoder-owned.
func validateJSONText(data []byte) error {
	if !utf8.Valid(data) {
		return fmt.Errorf("invalid UTF-8 in JSON")
	}
	for i := 0; i < len(data); i++ {
		if data[i] != '"' {
			continue
		}
		i++
		for ; i < len(data) && data[i] != '"'; i++ {
			if data[i] != '\\' {
				continue
			}
			i++
			if i >= len(data) || data[i] != 'u' {
				continue
			}
			if i+4 >= len(data) {
				return fmt.Errorf("incomplete Unicode escape")
			}
			code, err := strconv.ParseUint(string(data[i+1:i+5]), 16, 16)
			if err != nil {
				return fmt.Errorf("invalid Unicode escape: %w", err)
			}
			i += 4
			if code >= 0xdc00 && code <= 0xdfff {
				return fmt.Errorf("unpaired low Unicode surrogate")
			}
			if code < 0xd800 || code > 0xdbff {
				continue
			}
			if i+6 >= len(data) || data[i+1] != '\\' || data[i+2] != 'u' {
				return fmt.Errorf("unpaired high Unicode surrogate")
			}
			low, err := strconv.ParseUint(string(data[i+3:i+7]), 16, 16)
			if err != nil || low < 0xdc00 || low > 0xdfff {
				return fmt.Errorf("unpaired high Unicode surrogate")
			}
			i += 6
		}
	}
	return nil
}

// invalidGeneratedFieldTypeError is the value-codec adapter for shared checks.
func invalidGeneratedFieldTypeError(field, expected, actual, _ string) error {
	return fmt.Errorf("%s: expected %s, got %s", field, expected, actual)
}

// unknownJSONFieldError reports exact authored names, without case folding.
func unknownJSONFieldError(path, key string, _ []string) error {
	return fmt.Errorf("%s: unknown JSON field %q", path, key)
}

// decodedJSONType describes values produced only by the strict JSON reader.
func decodedJSONType(value any) string {
	switch value.(type) {
	case nil:
		return "null"
	case bool:
		return "boolean"
	case string:
		return "string"
	case json.Number:
		return "number"
	case []any:
		return "array"
	case map[string]any:
		return "object"
	default:
		return "invalid JSON value"
	}
}

// generatedJSONChildPath appends an unambiguous quoted member or array index.
func generatedJSONChildPath(path, key string, _ bool) string {
	return path + "[" + strconv.Quote(key) + "]"
}

// checkAnswerValue checks text and cycles before conversion.
func checkAnswerValue(in *Answer) error {
	if in == nil {
		return fmt.Errorf("missing root value")
	}
	active := make(map[any]bool)
	if err := checkAnswerAnswerValue(in, "$", active); err != nil {
		return err
	}
	return nil
}

// checkAnswerAnswerValue checks one generated value on the active path.
func checkAnswerAnswerValue(in *Answer, field string, active map[any]bool) error {
	if in == nil {
		return nil
	}
	if active[in] {
		return fmt.Errorf("%s: cyclic Go value", field)
	}
	active[in] = true
	defer delete(active, in)
	if in != nil {
		if !utf8.ValidString(string(in.Text)) {
			return fmt.Errorf("%s: invalid UTF-8", generatedJSONChildPath(field, "text", false))
		}
	}
	return nil
}

// checkAskPayloadValue checks text and cycles before conversion.
func checkAskPayloadValue(in *AskPayload) error {
	if in == nil {
		return fmt.Errorf("missing root value")
	}
	active := make(map[any]bool)
	if err := checkAskPayloadAskPayloadValue(in, "$", active); err != nil {
		return err
	}
	return nil
}

// checkAskPayloadAskPayloadValue checks one generated value on the active path.
func checkAskPayloadAskPayloadValue(in *AskPayload, field string, active map[any]bool) error {
	if in == nil {
		return nil
	}
	if active[in] {
		return fmt.Errorf("%s: cyclic Go value", field)
	}
	active[in] = true
	defer delete(active, in)
	if in != nil {
		if !utf8.ValidString(string(in.Question)) {
			return fmt.Errorf("%s: invalid UTF-8", generatedJSONChildPath(field, "question", false))
		}
	}
	return nil
}
