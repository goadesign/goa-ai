// Code generated by goa, DO NOT EDIT.
//
// helpers tool codecs
//
// Command:
// $ goa gen example.com/quickstart/design

package helpers

import (
	"bytes"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"sort"
	"strings"

	toolhttp "example.com/quickstart/gen/orchestrator/toolsets/helpers/http"
	"goa.design/goa-ai/runtime/agent/tools"
	goa "goa.design/goa/v3/pkg"
)

// AnswerPayloadCodec returns a fresh codec for *AnswerPayload.
func AnswerPayloadCodec() tools.JSONCodec[*AnswerPayload] {
	return tools.JSONCodec[*AnswerPayload]{
		ToJSON:   MarshalAnswerPayload,
		FromJSON: UnmarshalAnswerPayload,
	}
}

// AnswerResultCodec returns a fresh codec for *AnswerResult.
func AnswerResultCodec() tools.JSONCodec[*AnswerResult] {
	return tools.JSONCodec[*AnswerResult]{
		ToJSON:   MarshalAnswerResult,
		FromJSON: UnmarshalAnswerResult,
	}
}

var (
	// answerPayloadCodec provides an untyped codec for *AnswerPayload.
	answerPayloadCodec = tools.JSONCodec[any]{
		ToJSON: func(v any) ([]byte, error) {
			// Prefer typed marshal when the value matches the expected type.
			if typed, ok := v.(*AnswerPayload); ok {
				return MarshalAnswerPayload(typed)
			}
			return nil, fmt.Errorf("invalid value type for *AnswerPayload: %T", v)
		},
		FromJSON: func(data []byte) (any, error) {
			return UnmarshalAnswerPayload(data)
		},
	}
	// answerResultCodec provides an untyped codec for *AnswerResult.
	answerResultCodec = tools.JSONCodec[any]{
		ToJSON: func(v any) ([]byte, error) {
			// Prefer typed marshal when the value matches the expected type.
			if typed, ok := v.(*AnswerResult); ok {
				return MarshalAnswerResult(typed)
			}
			return nil, fmt.Errorf("invalid value type for *AnswerResult: %T", v)
		},
		FromJSON: func(data []byte) (any, error) {
			return UnmarshalAnswerResult(data)
		},
	}
)
var answerPayloadFieldDescs = map[string]string{
	"question": "User question to answer",
}
var answerResultFieldDescs = map[string]string{
	"text": "Answer text",
}
var answerPayloadFieldJSONTypes = map[string]string{
	"$payload": "object",
	"question": "string",
}
var answerResultFieldJSONTypes = map[string]string{
	"$payload": "object",
	"text":     "string",
}

// newValidationError converts a goa.ServiceError (possibly merged) into a
// tools.ValidationError with structured FieldIssue entries. It trims any leading
// "body." from field names for conciseness.
func newValidationError(err error) error {
	if err == nil {
		return nil
	}
	var se *goa.ServiceError
	if !errors.As(err, &se) {
		return err
	}
	hist := se.History()
	issues := make([]*tools.FieldIssue, 0, len(hist))
	for _, h := range hist {
		var field string
		if h.Field != nil {
			field = *h.Field
		}
		if strings.HasPrefix(field, "body.") {
			field = strings.TrimPrefix(field, "body.")
		}
		if field == "" {
			field = "$payload"
		}
		issues = append(issues, &tools.FieldIssue{Field: field, Constraint: h.Name})
	}
	if len(issues) == 0 {
		return err
	}
	return tools.NewValidationError(err.Error(), issues, nil)
}
func enrichAnswerPayloadValidationError(err error) error {
	var ve *tools.ValidationError
	if !errors.As(err, &ve) {
		return err
	}
	issues := ve.Issues()
	if len(issues) == 0 {
		return err
	}
	m := make(map[string]string)
	for _, is := range issues {
		if d, ok := tools.LookupFieldMetadata(answerPayloadFieldDescs, is.Field); ok && d != "" {
			m[is.Field] = d
		}
	}
	return tools.NewValidationError(ve.Error(), issues, m)
}
func enrichAnswerResultValidationError(err error) error {
	var ve *tools.ValidationError
	if !errors.As(err, &ve) {
		return err
	}
	issues := ve.Issues()
	if len(issues) == 0 {
		return err
	}
	m := make(map[string]string)
	for _, is := range issues {
		if d, ok := tools.LookupFieldMetadata(answerResultFieldDescs, is.Field); ok && d != "" {
			m[is.Field] = d
		}
	}
	return tools.NewValidationError(ve.Error(), issues, m)
}
func invalidAnswerPayloadFieldTypeError(err error) error {
	var typeErr *json.UnmarshalTypeError
	if !errors.As(err, &typeErr) {
		return err
	}
	field := typeErr.Field
	field = strings.TrimPrefix(field, "AnswerPayloadTransport.")
	if field == "" {
		field = "$payload"
	}
	expected, ok := tools.LookupFieldMetadata(answerPayloadFieldJSONTypes, field)
	if !ok {
		return err
	}
	actual := generatedUnmarshalJSONType(typeErr.Value)
	if actual == "" {
		return err
	}
	return tools.NewValidationError(
		err.Error(),
		[]*tools.FieldIssue{
			{
				Field:            field,
				Constraint:       "invalid_field_type",
				ExpectedJSONType: expected,
				ActualJSONType:   actual,
			},
		},
		nil,
	)
}
func invalidAnswerResultFieldTypeError(err error) error {
	var typeErr *json.UnmarshalTypeError
	if !errors.As(err, &typeErr) {
		return err
	}
	field := typeErr.Field
	field = strings.TrimPrefix(field, "AnswerResultTransport.")
	if field == "" {
		field = "$payload"
	}
	expected, ok := tools.LookupFieldMetadata(answerResultFieldJSONTypes, field)
	if !ok {
		return err
	}
	actual := generatedUnmarshalJSONType(typeErr.Value)
	if actual == "" {
		return err
	}
	return tools.NewValidationError(
		err.Error(),
		[]*tools.FieldIssue{
			{
				Field:            field,
				Constraint:       "invalid_field_type",
				ExpectedJSONType: expected,
				ActualJSONType:   actual,
			},
		},
		nil,
	)
}

// MarshalAnswerPayload serializes *AnswerPayload into JSON.
func MarshalAnswerPayload(v *AnswerPayload) ([]byte, error) {
	if v == nil {
		return nil, fmt.Errorf("answerPayload is nil")
	}
	in := v
	_ = in
	var out *toolhttp.AnswerPayloadTransport
	out = &toolhttp.AnswerPayloadTransport{
		Question: &in.Question,
	}
	return json.Marshal(out)
}

// UnmarshalAnswerPayload deserializes JSON into *AnswerPayload.
func UnmarshalAnswerPayload(data []byte) (*AnswerPayload, error) {
	if len(data) == 0 {
		return nil, fmt.Errorf("answerPayload JSON is empty")
	}
	var tv toolhttp.AnswerPayloadTransport
	if err := validateAnswerPayloadJSON(data); err != nil {
		return nil, invalidAnswerPayloadFieldTypeError(err)
	}
	if err := json.Unmarshal(data, &tv); err != nil {
		return nil, invalidAnswerPayloadFieldTypeError(err)
	}
	if err := toolhttp.ValidateAnswerPayloadTransport(&tv); err != nil {
		err = newValidationError(err)
		err = enrichAnswerPayloadValidationError(err)
		return nil, err
	}
	in := &tv
	_ = in
	var out *AnswerPayload
	out = &AnswerPayload{
		Question: *in.Question,
	}
	return out, nil
}

// MarshalAnswerResult serializes *AnswerResult into JSON.
func MarshalAnswerResult(v *AnswerResult) ([]byte, error) {
	if v == nil {
		return nil, fmt.Errorf("answerResult is nil")
	}
	in := v
	_ = in
	var out *toolhttp.AnswerResultTransport
	out = &toolhttp.AnswerResultTransport{
		Text: &in.Text,
	}
	return json.Marshal(out)
}

// UnmarshalAnswerResult deserializes JSON into *AnswerResult.
func UnmarshalAnswerResult(data []byte) (*AnswerResult, error) {
	if len(data) == 0 {
		return nil, fmt.Errorf("answerResult JSON is empty")
	}
	var tv toolhttp.AnswerResultTransport
	if err := validateAnswerResultJSON(data); err != nil {
		return nil, invalidAnswerResultFieldTypeError(err)
	}
	if err := json.Unmarshal(data, &tv); err != nil {
		return nil, invalidAnswerResultFieldTypeError(err)
	}
	if err := toolhttp.ValidateAnswerResultTransport(&tv); err != nil {
		err = newValidationError(err)
		err = enrichAnswerResultValidationError(err)
		return nil, err
	}
	in := &tv
	_ = in
	var out *AnswerResult
	out = &AnswerResult{
		Text: *in.Text,
	}
	return out, nil
}

// validateAnswerPayloadJSON parses one JSON document and checks the exact value
// shapes known from this generated Goa type.
func validateAnswerPayloadJSON(data []byte) error {
	var root any
	dec := json.NewDecoder(bytes.NewReader(data))
	dec.UseNumber()
	if err := dec.Decode(&root); err != nil {
		return err
	}
	if err := dec.Decode(&struct{}{}); err != io.EOF {
		return fmt.Errorf("multiple JSON documents")
	}
	return validateAnswerPayloadJSONValue("", root, "")
}

// validateAnswerResultJSON parses one JSON document and checks the exact value
// shapes known from this generated Goa type.
func validateAnswerResultJSON(data []byte) error {
	var root any
	dec := json.NewDecoder(bytes.NewReader(data))
	dec.UseNumber()
	if err := dec.Decode(&root); err != nil {
		return err
	}
	if err := dec.Decode(&struct{}{}); err != io.EOF {
		return fmt.Errorf("multiple JSON documents")
	}
	return validateAnswerResultJSONValue("", root, "")
}

// validateAnswerPayloadJSONValue checks one value whose JSON shape is fixed by the generated Goa type.
func validateAnswerPayloadJSONValue(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "object", "null", description)
	}
	typed, ok := value.(map[string]any)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "object", decodedJSONType(value), description)
	}
	keys := make([]string, 0, len(typed))
	for key := range typed {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		switch key {
		case "question":
			if err := validateStringJSONValue(
				generatedJSONChildPath(path, key, false),
				typed[key], "User question to answer",
			); err != nil {
				return err
			}
		default:
			return unknownJSONFieldError(path, key, []string{
				"question",
			})
		}
	}
	return nil
}

// validateStringJSONValue checks one value whose JSON shape is fixed by the generated Goa type.
func validateStringJSONValue(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "string", "null", description)
	}
	_, ok := value.(string)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "string", decodedJSONType(value), description)
	}
	return nil
}

// validateAnswerResultJSONValue checks one value whose JSON shape is fixed by the generated Goa type.
func validateAnswerResultJSONValue(path string, value any, description string) error {
	field := path
	if field == "" {
		field = "$payload"
	}
	if value == nil {
		return invalidGeneratedFieldTypeError(field, "object", "null", description)
	}
	typed, ok := value.(map[string]any)
	if !ok {
		return invalidGeneratedFieldTypeError(field, "object", decodedJSONType(value), description)
	}
	keys := make([]string, 0, len(typed))
	for key := range typed {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	for _, key := range keys {
		switch key {
		case "text":
			if err := validateStringJSONValue(
				generatedJSONChildPath(path, key, false),
				typed[key], "Answer text",
			); err != nil {
				return err
			}
		default:
			return unknownJSONFieldError(path, key, []string{
				"text",
			})
		}
	}
	return nil
}

// generatedJSONChildPath keeps fixed object fields dotted. After an array index
// or caller-defined map key, it uses slash-separated paths so later keys stay clear.
func generatedJSONChildPath(path, key string, useSlashPath bool) string {
	if useSlashPath || strings.HasPrefix(path, "/") {
		if path != "" && !strings.HasPrefix(path, "/") {
			path = dottedJSONPathPointer(path)
		}
		return path + "/" + escapeJSONPointerToken(key)
	}
	if path == "" {
		return key
	}
	return path + "." + key
}

// dottedJSONPathPointer converts the generated dotted path leading to an open
// map into JSON Pointer tokens.
func dottedJSONPathPointer(path string) string {
	parts := strings.Split(path, ".")
	var pointer strings.Builder
	for _, part := range parts {
		pointer.WriteByte('/')
		pointer.WriteString(escapeJSONPointerToken(part))
	}
	return pointer.String()
}

// escapeJSONPointerToken applies RFC 6901 escaping to one object key.
func escapeJSONPointerToken(token string) string {
	token = strings.ReplaceAll(token, "~", "~0")
	return strings.ReplaceAll(token, "/", "~1")
}

// decodedJSONType returns the JSON category retained by Decoder.UseNumber.
func decodedJSONType(value any) string {
	switch value.(type) {
	case bool:
		return "boolean"
	case json.Number:
		return "number"
	case string:
		return "string"
	case []any:
		return "array"
	case map[string]any:
		return "object"
	default:
		panic(fmt.Sprintf("unsupported decoded JSON value %T", value))
	}
}

// generatedUnmarshalJSONType reduces encoding/json's detailed value label to
// the stable JSON category carried by FieldIssue.
func generatedUnmarshalJSONType(value string) string {
	category, _, _ := strings.Cut(value, " ")
	switch category {
	case "array", "bool", "number", "object", "string":
		if category == "bool" {
			return "boolean"
		}
		return category
	default:
		return ""
	}
}

// invalidGeneratedFieldTypeError reports a raw JSON category that conflicts
// with the generated schema and attaches that schema path's description.
func invalidGeneratedFieldTypeError(field, expected, actual, description string) error {
	var descriptions map[string]string
	if description != "" {
		descriptions = map[string]string{field: description}
	}
	return tools.NewValidationError(
		fmt.Sprintf("field %q must be %s, got %s", field, expected, actual),
		[]*tools.FieldIssue{
			{
				Field:            field,
				Constraint:       "invalid_field_type",
				ExpectedJSONType: expected,
				ActualJSONType:   actual,
			},
		},
		descriptions,
	)
}

func unknownJSONFieldError(path, field string, allowed []string) error {
	issueField := generatedJSONChildPath(path, field, false)
	return tools.NewValidationError(
		fmt.Sprintf("unknown field %q", issueField),
		[]*tools.FieldIssue{
			{
				Field:      issueField,
				Constraint: "unknown_field",
				Allowed:    append([]string(nil), allowed...),
			},
		},
		nil,
	)
}
