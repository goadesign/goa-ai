// Code generated by goa v3.23.1, DO NOT EDIT.
//
// registry gRPC client CLI support package
//
// Command:
// $ goa gen goa.design/goa-ai/registry/design -o registry

package cli

import (
	"flag"
	"fmt"
	"os"

	registryc "goa.design/goa-ai/registry/gen/grpc/registry/client"
	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"registry (register|unregister|emit-tool-result|pong|list-toolsets|get-toolset|search|call-tool)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "registry register --message '{\n      \"description\": \"Tools for data processing and analysis\",\n      \"name\": \"data-tools\",\n      \"tags\": [\n         \"data\",\n         \"etl\",\n         \"analytics\"\n      ],\n      \"tools\": [\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         },\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         },\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         }\n      ],\n      \"version\": \"1.0.0\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	cc *grpc.ClientConn,
	opts ...grpc.CallOption,
) (goa.Endpoint, any, error) {
	var (
		registryFlags = flag.NewFlagSet("registry", flag.ContinueOnError)

		registryRegisterFlags       = flag.NewFlagSet("register", flag.ExitOnError)
		registryRegisterMessageFlag = registryRegisterFlags.String("message", "", "")

		registryUnregisterFlags       = flag.NewFlagSet("unregister", flag.ExitOnError)
		registryUnregisterMessageFlag = registryUnregisterFlags.String("message", "", "")

		registryEmitToolResultFlags       = flag.NewFlagSet("emit-tool-result", flag.ExitOnError)
		registryEmitToolResultMessageFlag = registryEmitToolResultFlags.String("message", "", "")

		registryPongFlags       = flag.NewFlagSet("pong", flag.ExitOnError)
		registryPongMessageFlag = registryPongFlags.String("message", "", "")

		registryListToolsetsFlags       = flag.NewFlagSet("list-toolsets", flag.ExitOnError)
		registryListToolsetsMessageFlag = registryListToolsetsFlags.String("message", "", "")

		registryGetToolsetFlags       = flag.NewFlagSet("get-toolset", flag.ExitOnError)
		registryGetToolsetMessageFlag = registryGetToolsetFlags.String("message", "", "")

		registrySearchFlags       = flag.NewFlagSet("search", flag.ExitOnError)
		registrySearchMessageFlag = registrySearchFlags.String("message", "", "")

		registryCallToolFlags       = flag.NewFlagSet("call-tool", flag.ExitOnError)
		registryCallToolMessageFlag = registryCallToolFlags.String("message", "", "")
	)
	registryFlags.Usage = registryUsage
	registryRegisterFlags.Usage = registryRegisterUsage
	registryUnregisterFlags.Usage = registryUnregisterUsage
	registryEmitToolResultFlags.Usage = registryEmitToolResultUsage
	registryPongFlags.Usage = registryPongUsage
	registryListToolsetsFlags.Usage = registryListToolsetsUsage
	registryGetToolsetFlags.Usage = registryGetToolsetUsage
	registrySearchFlags.Usage = registrySearchUsage
	registryCallToolFlags.Usage = registryCallToolUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "registry":
			svcf = registryFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "registry":
			switch epn {
			case "register":
				epf = registryRegisterFlags

			case "unregister":
				epf = registryUnregisterFlags

			case "emit-tool-result":
				epf = registryEmitToolResultFlags

			case "pong":
				epf = registryPongFlags

			case "list-toolsets":
				epf = registryListToolsetsFlags

			case "get-toolset":
				epf = registryGetToolsetFlags

			case "search":
				epf = registrySearchFlags

			case "call-tool":
				epf = registryCallToolFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "registry":
			c := registryc.NewClient(cc, opts...)
			switch epn {
			case "register":
				endpoint = c.Register()
				data, err = registryc.BuildRegisterPayload(*registryRegisterMessageFlag)
			case "unregister":
				endpoint = c.Unregister()
				data, err = registryc.BuildUnregisterPayload(*registryUnregisterMessageFlag)
			case "emit-tool-result":
				endpoint = c.EmitToolResult()
				data, err = registryc.BuildEmitToolResultPayload(*registryEmitToolResultMessageFlag)
			case "pong":
				endpoint = c.Pong()
				data, err = registryc.BuildPongPayload(*registryPongMessageFlag)
			case "list-toolsets":
				endpoint = c.ListToolsets()
				data, err = registryc.BuildListToolsetsPayload(*registryListToolsetsMessageFlag)
			case "get-toolset":
				endpoint = c.GetToolset()
				data, err = registryc.BuildGetToolsetPayload(*registryGetToolsetMessageFlag)
			case "search":
				endpoint = c.Search()
				data, err = registryc.BuildSearchPayload(*registrySearchMessageFlag)
			case "call-tool":
				endpoint = c.CallTool()
				data, err = registryc.BuildCallToolPayload(*registryCallToolMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// registryUsage displays the usage of the registry command and its subcommands.
func registryUsage() {
	fmt.Fprintln(os.Stderr, `Internal tool registry gateway for toolset discovery and tool invocation`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] registry COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    register: Register a toolset with the registry`)
	fmt.Fprintln(os.Stderr, `    unregister: Unregister a toolset from the registry`)
	fmt.Fprintln(os.Stderr, `    emit-tool-result: Emit a tool execution result to the result stream`)
	fmt.Fprintln(os.Stderr, `    pong: Respond to a health check ping`)
	fmt.Fprintln(os.Stderr, `    list-toolsets: List all registered toolsets with optional tag filtering`)
	fmt.Fprintln(os.Stderr, `    get-toolset: Get a specific toolset by name including all tool schemas`)
	fmt.Fprintln(os.Stderr, `    search: Search toolsets by keyword matching name, description, or tags`)
	fmt.Fprintln(os.Stderr, `    call-tool: Invoke a tool through the registry gateway`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s registry COMMAND --help\n", os.Args[0])
}
func registryRegisterUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry register", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Register a toolset with the registry`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry register --message '{\n      \"description\": \"Tools for data processing and analysis\",\n      \"name\": \"data-tools\",\n      \"tags\": [\n         \"data\",\n         \"etl\",\n         \"analytics\"\n      ],\n      \"tools\": [\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         },\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         },\n         {\n            \"description\": \"Analyze data and return insights\",\n            \"input_schema\": \"TnVsbGEgbm9iaXMu\",\n            \"name\": \"analyze\",\n            \"output_schema\": \"UmVydW0gYWNjdXNhbnRpdW0gYXNwZXJpb3JlcyBldCByYXRpb25lLg==\"\n         }\n      ],\n      \"version\": \"1.0.0\"\n   }'")
}

func registryUnregisterUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry unregister", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Unregister a toolset from the registry`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry unregister --message '{\n      \"name\": \"data-tools\"\n   }'")
}

func registryEmitToolResultUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry emit-tool-result", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Emit a tool execution result to the result stream`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry emit-tool-result --message '{\n      \"error\": {\n         \"code\": \"execution_failed\",\n         \"message\": \"Failed to connect to database\"\n      },\n      \"result\": \"Ab assumenda.\",\n      \"tool_use_id\": \"call-abc123\"\n   }'")
}

func registryPongUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry pong", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Respond to a health check ping`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry pong --message '{\n      \"ping_id\": \"ping-xyz789\",\n      \"toolset\": \"data-tools\"\n   }'")
}

func registryListToolsetsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry list-toolsets", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all registered toolsets with optional tag filtering`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry list-toolsets --message '{\n      \"tags\": [\n         \"data\",\n         \"etl\"\n      ]\n   }'")
}

func registryGetToolsetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry get-toolset", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get a specific toolset by name including all tool schemas`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry get-toolset --message '{\n      \"name\": \"data-tools\"\n   }'")
}

func registrySearchUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry search", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Search toolsets by keyword matching name, description, or tags`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry search --message '{\n      \"query\": \"data processing\"\n   }'")
}

func registryCallToolUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] registry call-tool", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Invoke a tool through the registry gateway`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "registry call-tool --message '{\n      \"payload\": \"Occaecati deleniti error repudiandae ab occaecati minima.\",\n      \"tool\": \"analyze\",\n      \"toolset\": \"data-tools\"\n   }'")
}
