// Package mongo provides a MongoDB-backed implementation of the runtime prompt
// store. Build the low-level client via features/prompt/mongo/clients/mongo and
// pass it to NewStore to persist and resolve scoped prompt overrides.
package mongo
