// Code generated by goa v3.22.6, DO NOT EDIT.
//
// orchestrator HTTP client CLI support package
//
// Command:
// $ goa gen example.com/assistant/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	orchestratorc "example.com/assistant/gen/http/orchestrator/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"orchestrator run",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "orchestrator run --body '{\n      \"agent_id\": \"orchestrator.chat\",\n      \"labels\": {\n         \"tenant\": \"acme\"\n      },\n      \"messages\": [\n         {\n            \"content\": \"You are a concise assistant.\",\n            \"meta\": {\n               \"message_id\": \"sys-1\"\n            },\n            \"role\": \"system\"\n         },\n         {\n            \"content\": \"Summarize the latest status update.\",\n            \"meta\": {\n               \"message_id\": \"user-1\"\n            },\n            \"role\": \"user\"\n         }\n      ],\n      \"metadata\": {\n         \"priority\": \"p1\"\n      },\n      \"run_id\": \"run-abc\",\n      \"session_id\": \"session-123\",\n      \"turn_id\": \"turn-7\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		orchestratorFlags = flag.NewFlagSet("orchestrator", flag.ContinueOnError)

		orchestratorRunFlags    = flag.NewFlagSet("run", flag.ExitOnError)
		orchestratorRunBodyFlag = orchestratorRunFlags.String("body", "REQUIRED", "")
	)
	orchestratorFlags.Usage = orchestratorUsage
	orchestratorRunFlags.Usage = orchestratorRunUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "orchestrator":
			svcf = orchestratorFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "orchestrator":
			switch epn {
			case "run":
				epf = orchestratorRunFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "orchestrator":
			c := orchestratorc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "run":
				endpoint = c.Run()
				data, err = orchestratorc.BuildRunPayload(*orchestratorRunBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// orchestratorUsage displays the usage of the orchestrator command and its
// subcommands.
func orchestratorUsage() {
	fmt.Fprintln(os.Stderr, `Agent service consuming the assistant MCP toolset`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] orchestrator COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    run: Invoke the chat agent`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s orchestrator COMMAND --help\n", os.Args[0])
}
func orchestratorRunUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] orchestrator run", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Invoke the chat agent`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "orchestrator run --body '{\n      \"agent_id\": \"orchestrator.chat\",\n      \"labels\": {\n         \"tenant\": \"acme\"\n      },\n      \"messages\": [\n         {\n            \"content\": \"You are a concise assistant.\",\n            \"meta\": {\n               \"message_id\": \"sys-1\"\n            },\n            \"role\": \"system\"\n         },\n         {\n            \"content\": \"Summarize the latest status update.\",\n            \"meta\": {\n               \"message_id\": \"user-1\"\n            },\n            \"role\": \"user\"\n         }\n      ],\n      \"metadata\": {\n         \"priority\": \"p1\"\n      },\n      \"run_id\": \"run-abc\",\n      \"session_id\": \"session-123\",\n      \"turn_id\": \"turn-7\"\n   }'")
}
