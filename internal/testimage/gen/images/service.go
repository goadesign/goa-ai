// Code generated by goa, DO NOT EDIT.
//
// images service
//
// Command:
// $ goa gen goa.design/goa-ai/internal/testimage/design -o internal/testimage

package images

import "context"

// Service is the images service interface.
type Service interface {
	// View implements view.
	View(context.Context, *ImageSelection) (res *ViewResult, err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "image-source-fixture"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "images"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [1]string{"view"}

type ImageSelected struct {
	// Exact selected image identity.
	ID string
}

// ImageSelection is the payload type of the images service view method.
type ImageSelection struct {
	// Exact image selected for inspection.
	ID string
}

// Immutable image identity supplied by the fixture's retained owner.
type ImageSource struct {
	// Exact retained image identity.
	ID string
	// Accepted image encoding.
	Format string
	// Accepted image byte length.
	Size int64
	// SHA-256 of the accepted bytes.
	Sha256 string
}

// ViewResult is the result type of the images service view method.
type ViewResult struct {
	// Exact selected image identity.
	ID string
	// Server-owned immutable image descriptor.
	Source *ImageSource
}
