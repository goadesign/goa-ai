// Code generated by goa, DO NOT EDIT.
//
// pictures tool transport types
//
// Command:
// $ goa gen goa.design/goa-ai/internal/testimage/design -o internal/testimage

package http

type (
	// ViewPayloadTransport is the internal JSON transport type for ViewPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewPayloadTransport struct {
		// Exact image selected for inspection.
		ID *string `json:"id"`
	}
	// ViewResultTransport is the internal JSON transport type for ViewResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewResultTransport struct {
		// Exact selected image identity.
		ID *string `json:"id"`
	}
	// ViewFixtureImageV1ServerDataTransport is the internal JSON transport type for ViewFixtureImageV1ServerData.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewFixtureImageV1ServerDataTransport struct {
		// Exact retained image identity.
		ID *string `json:"id"`
		// Accepted image encoding.
		Format *string `json:"format"`
		// Accepted image byte length.
		Size *int64 `json:"size"`
		// SHA-256 of the accepted bytes.
		Sha256 *string `json:"sha256"`
	}
)
