// Code generated by goa, DO NOT EDIT.
//
// pictures tool transport types
//
// Command:
// $ goa gen goa.design/goa-ai/internal/testimageshared/design -o
// internal/testimageshared

package http

type (
	// InspectPayloadTransport is the internal JSON transport type for InspectPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	InspectPayloadTransport struct {
		// Selected image.
		ID *string `json:"id"`
	}
	// InspectResultTransport is the internal JSON transport type for InspectResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	InspectResultTransport struct {
		// Exact selected image.
		Source *RetainedImageTransport `json:"source"`
	}
	// InspectFixtureSharedV1ServerDataTransport is the internal JSON transport type for InspectFixtureSharedV1ServerData.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	InspectFixtureSharedV1ServerDataTransport struct {
		// Exact retained image.
		ID *string `json:"id"`
	}
	// ViewPayloadTransport is the internal JSON transport type for ViewPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewPayloadTransport struct {
		// Selected image.
		ID *string `json:"id"`
	}
	// ViewResultTransport is the internal JSON transport type for ViewResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewResultTransport struct {
		// Exact selected image.
		Source *RetainedImageTransport `json:"source"`
	}
	// ViewFixtureSharedV1ServerDataTransport is the internal JSON transport type for ViewFixtureSharedV1ServerData.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ViewFixtureSharedV1ServerDataTransport struct {
		// Exact retained image.
		ID *string `json:"id"`
	}
	// RetainedImageTransport is the internal JSON transport type for RetainedImageTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	RetainedImageTransport struct {
		// Exact retained image.
		ID *string `json:"id"`
	}
)
