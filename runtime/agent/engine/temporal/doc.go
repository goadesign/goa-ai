// Package temporal implements the goa-ai workflow engine adapter backed by
// Temporal. It satisfies the generic engine.Engine interface so generated code
// and the runtime can register workflows/activities without importing the
// Temporal SDK directly.
package temporal
