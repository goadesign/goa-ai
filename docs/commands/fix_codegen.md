Goal: fix the code generation algorithm so that AURA generates properly

  Instructions
  Execute each step below in order, do not skip
  1. Run "go test ./..." in "~/src/aura/gen"
  2. Review the errors
  3. Fix the codegen algos in goa-ai to resolve the errors
  4. Run "scripts/gen goa" in "~/src/aura"
  5. Go to 1

  Repeat until "go test ./..." finishes cleanly.
  Remember to apply the coding guidelines when making changes. Be judicious, write elegant code.