scenarios:
  - name: prompts_get_invalid_cli
    defaults:
      client: jsonrpc.mcp_assistant
      client_mode: cli
    pre:
      auto_initialize: true
    steps:
      - name: get
        op: PromptsGet
        input:
          name: contextual_prompts
          arguments: { context: testing }
        expect:
          status: error


