// Code generated by goa vX, DO NOT EDIT.
//
// union tool transport types
//
// Command:
// goa

package http

type (
	// EchoPayloadTransport is the internal JSON transport type for EchoPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EchoPayloadTransport struct {
		// Request identifier
		ID            *string        `json:"id"`
		Value         *Value         `json:"value"`
		OptionalValue *OptionalValue `json:"optional_value,omitempty"`
	}
	// EchoResultTransport is the internal JSON transport type for EchoResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EchoResultTransport struct {
		// Request identifier
		ID            *string        `json:"id"`
		Value         *Value         `json:"value"`
		OptionalValue *OptionalValue `json:"optional_value,omitempty"`
	}
	// OptionalValueNumberTransport is the internal JSON transport type for OptionalValueNumberTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	OptionalValueNumberTransport int32
	// OptionalValueTextTransport is the internal JSON transport type for OptionalValueTextTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	OptionalValueTextTransport string
	// StructuredValueTransport is the internal JSON transport type for StructuredValueTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	StructuredValueTransport struct {
		// Structured value label
		Label *string `json:"label"`
	}
	// ValueNumberTransport is the internal JSON transport type for ValueNumberTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ValueNumberTransport int32
	// ValueTextTransport is the internal JSON transport type for ValueTextTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ValueTextTransport string
)
