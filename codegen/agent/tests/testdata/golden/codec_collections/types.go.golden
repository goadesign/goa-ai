// Code generated by goa vX, DO NOT EDIT.
//
// collections tool types
//
// Command:

package collections

type (
	// ArchivePayload defines the JSON payload for the collections.archive tool.
	ArchivePayload = struct {
		// Archived aliases.
		Aliases []Alias
		// Archived integers.
		Numbers []int32
		// Archived unsigned integers.
		UnsignedNumbers []uint32
		// Archived large integers.
		LargeNumbers []int64
		// Archived counts by name.
		Counts map[string]int32
		// Archived counts grouped by position.
		Groups []map[string]int32
		// Archived recursive node.
		Node *Node
		// Archived unrestricted JSON value.
		Dynamic any
	}
	// StorePayload defines the JSON payload for the collections.store tool.
	StorePayload = struct {
		// Required aliases.
		Aliases []Alias
		// Required integers.
		Numbers []int32
		// Unsigned integers.
		UnsignedNumbers []uint32
		// Required large integers.
		LargeNumbers []int64
		// Integer counts by name.
		Counts map[string]int32
		// Integer counts grouped by position.
		Groups []map[string]int32
		// Recursive node.
		Node *Node
		// Unrestricted JSON value.
		Dynamic any
	}
	// StoreResult defines the JSON result for the collections.store tool.
	StoreResult = struct {
		// Required aliases.
		Aliases []Alias
		// Required integers.
		Numbers []int32
		// Unsigned integers.
		UnsignedNumbers []uint32
		// Required large integers.
		LargeNumbers []int64
		// Integer counts by name.
		Counts map[string]int32
		// Integer counts grouped by position.
		Groups []map[string]int32
		// Recursive node.
		Node *Node
		// Unrestricted JSON value.
		Dynamic any
	}
	// Alias is a nested type used by the generated JSON contract.
	Alias = string
	// Node is a nested type used by the generated JSON contract.
	Node struct {
		// Node name.
		Name string
		// Next node.
		Next *Node
	}
)
