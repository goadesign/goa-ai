// Code generated by goa vX, DO NOT EDIT.
//
// tasks completion codecs
//
// Command:

package completions

import (
	"encoding/json"
	"errors"
	"fmt"
	"goa.design/goa-ai/runtime/agent/tools"
	toolhttp "goa.design/goa-ai/tasks/completions/http"
	goa "goa.design/goa/v3/pkg"
	"strings"
)

var (
	// DraftFromTranscriptResultCodec serializes values of type *DraftFromTranscriptResult to canonical JSON.
	DraftFromTranscriptResultCodec = tools.JSONCodec[*DraftFromTranscriptResult]{
		ToJSON:   MarshalDraftFromTranscriptResult,
		FromJSON: UnmarshalDraftFromTranscriptResult,
	}
	// draftFromTranscriptResultCodec provides an untyped codec for *DraftFromTranscriptResult.
	draftFromTranscriptResultCodec = tools.JSONCodec[any]{
		ToJSON: func(v any) ([]byte, error) {
			// Prefer typed marshal when the value matches the expected type.
			if typed, ok := v.(*DraftFromTranscriptResult); ok {
				return MarshalDraftFromTranscriptResult(typed)
			}
			return nil, fmt.Errorf("invalid value type for *DraftFromTranscriptResult: %T", v)
		},
		FromJSON: func(data []byte) (any, error) {
			return UnmarshalDraftFromTranscriptResult(data)
		},
	}
)
var DraftFromTranscriptResultFieldDescs = map[string]string{
	"assistant_text":          "Assistant summary",
	"draft.value.steps":       "Draft steps",
	"draft.value.steps.label": "User-visible step label",
}

// ValidationError wraps a validation failure and exposes issues that callers
// can use to build retry hints. It implements error and an Issues() accessor.
type ValidationError struct {
	msg          string
	issues       []*tools.FieldIssue
	descriptions map[string]string
}

func (e ValidationError) Error() string {
	return e.msg
}
func (e ValidationError) Issues() []*tools.FieldIssue {
	if len(e.issues) == 0 {
		return nil
	}
	out := make([]*tools.FieldIssue, len(e.issues))
	copy(out, e.issues)
	return out
}
func (e ValidationError) Descriptions() map[string]string {
	if len(e.descriptions) == 0 {
		return nil
	}
	out := make(map[string]string, len(e.descriptions))
	for k, v := range e.descriptions {
		out[k] = v
	}
	return out
}

// newValidationError converts a goa.ServiceError (possibly merged) into a
// ValidationError with structured FieldIssue entries. It trims any leading
// "body." from field names for conciseness.
func newValidationError(err error) error {
	if err == nil {
		return nil
	}
	var se *goa.ServiceError
	if !errors.As(err, &se) {
		return err
	}
	hist := se.History()
	issues := make([]*tools.FieldIssue, 0, len(hist))
	for _, h := range hist {
		var field string
		if h.Field != nil {
			field = *h.Field
		}
		if strings.HasPrefix(field, "body.") {
			field = strings.TrimPrefix(field, "body.")
		}
		issues = append(issues, &tools.FieldIssue{Field: field, Constraint: h.Name})
	}
	if len(issues) == 0 {
		return err
	}
	return &ValidationError{
		msg:    err.Error(),
		issues: issues,
	}
}
func enrichDraftFromTranscriptResultValidationError(err error) error {
	ve, ok := err.(*ValidationError)
	if !ok || ve == nil {
		return err
	}
	if len(ve.issues) == 0 {
		return err
	}
	m := make(map[string]string)
	for _, is := range ve.issues {
		if d, ok := DraftFromTranscriptResultFieldDescs[is.Field]; ok && d != "" {
			m[is.Field] = d
		}
	}
	ve.descriptions = m
	return ve
}

// MarshalDraftFromTranscriptResult serializes *DraftFromTranscriptResult into JSON.
func MarshalDraftFromTranscriptResult(v *DraftFromTranscriptResult) ([]byte, error) {
	if v == nil {
		return nil, fmt.Errorf("draftFromTranscriptResult is nil")
	}
	in := v
	_ = in
	var out *toolhttp.DraftFromTranscriptResultTransport
	out = &toolhttp.DraftFromTranscriptResultTransport{
		AssistantText: &in.AssistantText,
	}
	switch string(in.Draft.Kind()) {
	case "new_task":
		actual, _ := in.Draft.AsNewTask()
		obj := encodeCompletionDraftToToolhttpCompletionDraftTransport(actual)
		u := out.Draft
		u.SetNewTask((*toolhttp.CompletionDraftTransport)(obj))
		out.Draft = u
	case "existing_task_id":
		actual, _ := in.Draft.AsExistingTaskID()
		obj := toolhttp.DraftExistingTaskIDTransport(actual)

		u := out.Draft
		u.SetExistingTaskID((toolhttp.DraftExistingTaskIDTransport)(obj))
		out.Draft = u
	}
	return json.Marshal(out)
}

// UnmarshalDraftFromTranscriptResult deserializes JSON into *DraftFromTranscriptResult.
func UnmarshalDraftFromTranscriptResult(data []byte) (*DraftFromTranscriptResult, error) {
	if len(data) == 0 {
		return nil, fmt.Errorf("draftFromTranscriptResult JSON is empty")
	}
	var tv toolhttp.DraftFromTranscriptResultTransport
	if err := json.Unmarshal(data, &tv); err != nil {
		return nil, fmt.Errorf("decode draftFromTranscriptResult: %w", err)
	}
	if err := toolhttp.ValidateDraftFromTranscriptResultTransport(&tv); err != nil {
		err = newValidationError(err)
		err = enrichDraftFromTranscriptResultValidationError(err)
		return nil, err
	}
	in := &tv
	_ = in
	var out *DraftFromTranscriptResult
	out = &DraftFromTranscriptResult{
		AssistantText: *in.AssistantText,
	}
	switch string(in.Draft.Kind()) {
	case "new_task":
		actual, _ := in.Draft.AsNewTask()
		obj := decodeToolhttpCompletionDraftTransportToCompletionDraft(actual)
		u := out.Draft
		u.SetNewTask((*CompletionDraft)(obj))
		out.Draft = u
	case "existing_task_id":
		actual, _ := in.Draft.AsExistingTaskID()
		obj := DraftExistingTaskID(actual)

		u := out.Draft
		u.SetExistingTaskID((DraftExistingTaskID)(obj))
		out.Draft = u
	}
	return out, nil
}

// Helper transform functions
func decodeToolhttpCompletionDraftTransportToCompletionDraft(v *toolhttp.CompletionDraftTransport) *CompletionDraft {
	res := &CompletionDraft{}
	res.Steps = make([]*CompletionStep, len(v.Steps))
	for i, val := range v.Steps {
		if val == nil {
			res.Steps[i] = nil
			continue
		}
		res.Steps[i] = decodeToolhttpCompletionStepTransportToCompletionStep(val)
	}

	return res
}
func decodeToolhttpCompletionStepTransportToCompletionStep(v *toolhttp.CompletionStepTransport) *CompletionStep {
	res := &CompletionStep{
		Label: *v.Label,
	}

	return res
}
func encodeCompletionDraftToToolhttpCompletionDraftTransport(v *CompletionDraft) *toolhttp.CompletionDraftTransport {
	res := &toolhttp.CompletionDraftTransport{}
	res.Steps = make([]*toolhttp.CompletionStepTransport, len(v.Steps))
	for i, val := range v.Steps {
		if val == nil {
			res.Steps[i] = nil
			continue
		}
		res.Steps[i] = encodeCompletionStepToToolhttpCompletionStepTransport(val)
	}

	return res
}
func encodeCompletionStepToToolhttpCompletionStepTransport(v *CompletionStep) *toolhttp.CompletionStepTransport {
	res := &toolhttp.CompletionStepTransport{
		Label: &v.Label,
	}

	return res
}
