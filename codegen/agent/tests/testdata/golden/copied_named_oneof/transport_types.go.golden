// Code generated by goa vX, DO NOT EDIT.
//
// records tool transport types
//
// Command:
// goa

package http

type (
	// EvaluatePayloadTransport is the internal JSON transport type for EvaluatePayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EvaluatePayloadTransport struct {
	}
	// EvaluateResultTransport is the internal JSON transport type for EvaluateResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	EvaluateResultTransport struct {
		// First result and its selected filter.
		First *FirstResultTransport `json:"first"`
		// Second result and its selected filter.
		Second *SecondResultTransport `json:"second"`
	}
	// FilterNameTransport is the internal JSON transport type for FilterNameTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FilterNameTransport string
	// FirstResultTransport is the internal JSON transport type for FirstResultTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FirstResultTransport struct {
		// Filter used for the first result.
		Filter *SharedFilterTransport `json:"filter"`
	}
	// MatchAllTransport is the internal JSON transport type for MatchAllTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	MatchAllTransport struct {
	}
	// SecondResultTransport is the internal JSON transport type for SecondResultTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SecondResultTransport struct {
		// Filter used for the second result.
		Filter *SharedFilterTransport `json:"filter"`
	}
	// SharedFilterTransport is the internal JSON transport type for SharedFilterTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SharedFilterTransport struct {
		// Selected filter value.
		Filter *Filter `json:"filter"`
	}
)
