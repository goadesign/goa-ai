// Code generated by goa vX, DO NOT EDIT.
//
// filters tool transport types
//
// Command:

package http

type (
	// FirstPayloadTransport is the internal JSON transport type for FirstPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FirstPayloadTransport struct {
		Primary *FilterTransport `json:"primary"`
		Copy    *FilterTransport `json:"copy"`
	}
	// SecondPayloadTransport is the internal JSON transport type for SecondPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SecondPayloadTransport struct {
		Filter *FilterTransport2 `json:"filter"`
	}
	// FilterTransport is the internal JSON transport type for FilterTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FilterTransport struct {
		Value *string `json:"value"`
	}
	// FilterTransport2 is the internal JSON transport type for FilterTransport2.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	FilterTransport2 struct {
		Value *string `json:"value"`
	}
)
