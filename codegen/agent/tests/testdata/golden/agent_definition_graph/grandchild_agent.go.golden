// Code generated by goa vX, DO NOT EDIT.
//
// RootAgent implementation
//
// Command:
// goa

package root

import (
	specs "generated.local/gen/entry/agents/root/specs"
	specs2 "generated.local/gen/leaf/agents/grandchild/specs"
	specs3 "generated.local/gen/middle/agents/child/specs"
	agent "goa.design/goa-ai/runtime/agent"
	"goa.design/goa-ai/runtime/agent/planner"
	runtime "goa.design/goa-ai/runtime/agent/runtime"
	"goa.design/goa-ai/runtime/agent/tools"
)

// AgentID is the fully-qualified identifier for this agent.
const AgentID agent.Ident = "entry.root"

// Workflow and activity identifiers for this agent.
const (
	// WorkflowName is the fully-qualified workflow identifier registered with the engine.
	WorkflowName = "entry.root.workflow"
	// DefaultTaskQueue is the engine queue this agent polls for workflow and activity tasks.
	DefaultTaskQueue = "entry_root_workflow"
	// PlanActivity is the activity name that runs the initial planning turn.
	PlanActivity = "entry.root.plan"
	// ResumeActivity is the activity name that runs the resume turn after tool execution.
	ResumeActivity = "entry.root.resume"
	// ExecuteToolActivity is the activity name used to execute tools via the engine.
	ExecuteToolActivity = "entry.root.executetool"
)

// RootAgent wraps the planner implementation for agent "root".
type RootAgent struct {
	Planner planner.Planner
}

// NewRootAgent validates the configuration and constructs a RootAgent.
func NewRootAgent(cfg RootAgentConfig) (*RootAgent, error) {
	if err := cfg.Validate(); err != nil {
		return nil, err
	}
	return &RootAgent{Planner: cfg.Planner}, nil
}

var agentDefinition = runtime.NewAgentDefinition(
	runtime.AgentRoute{
		ID:               AgentID,
		WorkflowName:     WorkflowName,
		DefaultTaskQueue: DefaultTaskQueue,
	},
	specs.Specs(),
	specs.MetadataByName,
	specs.RequiredLabels(),
	[]tools.Ident{
		tools.Ident("child_work.delegate"),
	},
	[]runtime.AgentDefinition{
		runtime.NewAgentDefinition(
			runtime.AgentRoute{
				ID:               agent.Ident("leaf.grandchild"),
				WorkflowName:     "leaf.grandchild.workflow",
				DefaultTaskQueue: "leaf_grandchild_workflow",
			},
			specs2.Specs(),
			specs2.MetadataByName,
			specs2.RequiredLabels(),
			[]tools.Ident{},
			nil,
		),
		runtime.NewAgentDefinition(
			runtime.AgentRoute{
				ID:               agent.Ident("middle.child"),
				WorkflowName:     "middle.child.workflow",
				DefaultTaskQueue: "middle_child_workflow",
			},
			specs3.Specs(),
			specs3.MetadataByName,
			specs3.RequiredLabels(),
			[]tools.Ident{
				tools.Ident("grandchild_work.finish"),
			},
			nil,
		),
	},
)

// Definition returns the immutable generated contract shared by callers and workers.
func Definition() runtime.AgentDefinition {
	return agentDefinition
}

// NewClient returns a runtime.AgentClient bound to this agent. In caller
// processes that do not register the agent locally, it still validates starts
// against the same generated contract as the worker.
func NewClient(rt *runtime.Runtime) runtime.AgentClient {
	return rt.MustClientFor(Definition())
}
