// Code generated by goa vX, DO NOT EDIT.
//
// progress tool transport types
//
// Command:

package http

type (
	// SetStepStatusPayloadTransport is the internal JSON transport type for SetStepStatusPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	SetStepStatusPayloadTransport struct {
		// Status event to record.
		Event *StatusChangedEventTransport `json:"event"`
	}
	// ActiveStatusTransport is the internal JSON transport type for ActiveStatusTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ActiveStatusTransport struct {
		// Current lifecycle status.
		Status *StatusTransport `json:"status"`
	}
	// InactiveStatusTransport is the internal JSON transport type for InactiveStatusTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	InactiveStatusTransport struct {
	}
	// StatusChangedEventTransport is the internal JSON transport type for StatusChangedEventTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	StatusChangedEventTransport struct {
		// Step identifier.
		StepID *string `json:"step_id"`
		// New step status.
		Status *StatusTransport `json:"status"`
		// Optional note about progress.
		Note *string `json:"note,omitempty"`
		// Selected step state.
		State *StepState `json:"state"`
	}
	// StatusTransport is the internal JSON transport type for StatusTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	StatusTransport string
	// StepStateSinceTransport is the internal JSON transport type for StepStateSinceTransport.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	StepStateSinceTransport string
)
